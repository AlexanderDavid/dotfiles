#!/usr/bin/env sh

[ "$(cat /sys/class/net/w*/operstate)" = 'down' ] && wifiicon=""

[ ! -n "${wifiicon+var}" ] && wifiicon=$(grep "^\s*w" /proc/net/wireless | awk '{ print " ", sprintf("%03s", int($3 * 100 / 70)) "%" }')

printf "%s " "$wifiicon" # "$(cat /sys/class/net/w*/operstate | sed "s/down/⬇/;s/up//")"
